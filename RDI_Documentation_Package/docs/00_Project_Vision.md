# Project Vision

## Vision
Build a cross-platform Redis Discovery & Inventory utility that safely discovers Redis-compatible databases on authorized networks and provides read-only inventory through a CLI and lightweight Web UI.

## Principles
- Read-only by default
- No credential persistence
- Local-first
- Cross-platform
- Protocol-aware discovery
- Operator-friendly
